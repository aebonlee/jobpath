# ForJob - 디자인 점검 및 상단메뉴 아이콘 제거

**작성일:** 2026-04-05
**프로젝트:** ForJob (직업상담사 2급 시험 준비 플랫폼)
**커밋:** 48adc95

---

## 개요

전체 CSS 디자인을 점검하고, ��단 네비게이션 메뉴의 FontAwesome 아이콘을 제거했습니다.
CSS 누락 및 클래스명 불일치로 인한 디자인 버그 3건을 수정했습니다.

---

## 1. 상단메뉴 아이콘 제거

### 변경 파일: `src/components/layout/Navbar.jsx`

| 위치 | 변경 전 | 변경 후 |
|------|---------|---------|
| 데스크탑 nav-link (line 62) | `<i className={item.icon} /> {item.label}` | `{item.label}` |
| 모바일 subnav-toggle (line 141) | `<i className={item.icon} style={{marginRight:8}} /> {item.label}` | `{item.label}` |
| 모바일 nav-link (line 159) | `<i className={item.icon} style={{marginRight:8}} /> {item.label}` | `{item.label}` |
| 로그��� 링크 (line 115) | `<i className="fa-solid fa-right-to-bracket" /> 로그인` | `로그인` |

- 테마 토글 아이콘(◑/☀️/🌙)은 기능 버튼이므로 유지

---

## 2. 디자��� 버그 수정 (3건)

### Bug 1: `home-subjects-grid` / `home-subject-card` CSS 누락

- **증상:** Home.jsx에서 `home-subjects-grid`, `home-subject-card` 클래스를 사용하지��� CSS 정의 없음
- **원인:** home.css에 해당 클래스 미작성
- **수정:** home.css에 subjects 그리드 CSS 추가 (5열 → 3열 → 2열 반응형)
- **추가:** dark-mode.css, responsive.css에도 대응 스타일 추가

### Bug 2: Stats 섹션 배경 누락 (white on white)

- **증상:** Home.jsx 통계 섹션의 ���스트가 흰색인데 배경도 흰색 → 텍스트 안 보임
- **원인:** CSS에 `home-stats-section` (그라디언트 배경)이 정의되어 있으나, JSX에서 `home-section` 클래스 사용
- **수정:** `<section className="home-section">` → `<section className="home-stats-section">`

### Bug 3: CTA 섹�� 클래스 ���일치

- **증상:** CTA 텍스트도 흰색인데 배경 없음
- **원인:** JSX `className="home-cta"` ≠ CSS `.home-cta-section`
- **수정:** `<section className="home-cta">` → `<section className="home-cta-section">`

### Bug 4: `home-stat-item` 클래스 미정의

- **수정:** home.css에 `.home-stat-item` flex column 스타일 추가

---

## 변경 파일 요약

| 파일 | 변경 내용 |
|------|----------|
| `src/components/layout/Navbar.jsx` | FontAwesome 아이콘 4곳 제거 |
| `src/pages/Home.jsx` | Stats/CTA 섹션 클래스명 수정 |
| `src/styles/home.css` | subjects 그리드 CSS 추가, stat-item 추가 |
| `src/styles/dark-mode.css` | subject 카드 다크모드 |
| `src/styles/responsive.css` | subjects 그리드 반응형 (태블릿 3열, 모바일 2열) |
